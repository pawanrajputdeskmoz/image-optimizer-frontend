/** Shown at the top of the Home images tab. */
export const HOME_CAROUSEL_UPDATE_NOTE =
  "Images marked with * come from a BigCommerce Carousel (Page Builder widget). BigCommerce does not provide an API to update carousel slide images, so those items cannot be optimized or applied automatically — use Preview to compare, then replace the image manually in Page Builder.";

/** Tooltip / title on the row-level star indicator. */
export const CAROUSEL_ROW_NOTE_TITLE =
  "Carousel images cannot be updated via the BigCommerce API. Optimization is not available for this image.";
